USE_LLM=false

# USE_LLM=true 时启用大模型生成；未配置或调用失败会退回本地模板。
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1/chat/completions
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=3000

# 可选：每日摘要通知。支持 generic、feishu、lark、wecom、dingtalk。
SUMMARY_WEBHOOK_URL=
SUMMARY_WEBHOOK_TYPE=generic
